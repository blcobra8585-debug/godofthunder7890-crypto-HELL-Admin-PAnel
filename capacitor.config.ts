import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hell.ssengineering',
  appName: 'HELL',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
